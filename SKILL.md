---
name: tech-doc-style-chinese-for-ql
description: 在撰写、改写、校对或审阅 Markdown 文档时使用，尤其当文档将进入 MarkdownQLite 或兼容渲染器预览时。继承 tech-doc-style-chinese 的克制、准确、可扫读中文写作规范，并追加 MarkdownQLite 渲染契约输出补丁——多属性内容必用 frontmatter 元数据标记、链接按显式 scheme 书写、wikilink 双链、数学公式按受支持命令集书写。按任务读取术语排版、渲染契约、元数据或受控中文技术写作参考。
---

# 中文技术文档与产品文案规范（MarkdownQLite 衍生版）

<!-- 上游：tech-doc-style-chinese，作者 Fenng（GitHub：@Fenng），MIT License -->
<!-- 本分支：MarkdownQLite 渲染契约输出补丁，见 references/render-contract.md 与 UPSTREAM.md -->

本技能是 `tech-doc-style-chinese`（作者 Fenng，MIT License）的衍生分支，在原规范之上追加 MarkdownQLite 渲染契约的输出补丁，使按本技能产出的 Markdown 文档都能在 MarkdownQLite 中正确渲染。

## 与上游的关系

- 继承上游全部写作规范：事实保真、任务模式判断、核心写作规则、按内容类型处理、编辑流程。
- 追加 MarkdownQLite 渲染契约：见「MarkdownQLite 渲染契约输出补丁」一节。
- 上游更新通过 `UPSTREAM.md` 记录的快照哈希清单检测与合并；上游规则与渲染契约冲突时，保留事实与机器可读内容优先（与上游规则优先级一致），渲染约束次之。

## 适用范围

将本 Skill 用于中文内容的撰写、改写、校对和审阅，包括：

- 文档首页、产品介绍、解决方案页和更新日志
- API 文档、参数说明、错误码和常见问题
- 操作手册、故障排查、运维 Runbook 和安全说明
- 界面文案、按钮、导航、状态和提示信息
- 需要在 MarkdownQLite 中预览或发布的任何 Markdown 文档

不要改写代码字面量、JSON 键名、URL、API 路径、数据库字段名、命令、配置项或其他机器可读标识符。

## 规则优先级

发生冲突时，按以下顺序处理：

1. 保留事实、逻辑、限制、安全信息和法律含义。
2. 服从用户明确要求和目标项目约定。
3. 保持技术术语和机器可读内容准确。
4. 满足 MarkdownQLite 渲染契约（见下节与 references/render-contract.md）。
5. 改善结构、语义、语气和可扫读性。
6. 最后处理标点、留白、大小写等排版细节。

不得为了句子更短、语气更确定或格式更统一而牺牲更高优先级的信息。

## 先判断任务模式

### 撰写

- 先确定受众、内容类型、事实来源、发布载体和篇幅。
- 发布载体为 MarkdownQLite 或其兼容渲染器时，读取 references/render-contract.md 并执行其输出补丁。
- 缺少会改变结论的事实时，先标记缺口，再继续组织可确认内容。

### 改写

- 保留事实、逻辑关系、信息层级、限制和必要例外。
- 默认交付完整改写稿；重大语义选择或待确认内容另行说明。
- 交付前按渲染契约自检：元数据、链接 scheme、wikilink、数学公式、frontmatter。

### 校对

- 只处理约定范围内的错字、标点、留白、大小写和术语一致性。
- 未经要求，不改变结构、语气或事实表达。
- 发现违反渲染契约的写法时，单独列出，不并入校对改动。

### 审阅

- 先列问题，再给建议；按影响程度排序并引用具体原文。
- 审阅以 MarkdownQLite 为发布载体的文档时，同时对照渲染契约与写作规范。
- 未经授权，不直接修改文件。

## 核心写作规则

### 语义与语气

- 准确先于修辞，清晰先于热闹。
- 使用克制、直接、可执行的中文。
- 优先说明「是什么、适用于什么、需要做什么、接着看哪里」。
- 避免问候式开场、宣传口号、空泛形容和连续堆叠黑话。
- 默认不直接称呼读者；必要时使用明确角色，如「开发者」「实施人员」。
- 第二人称、感叹号和品牌语气属于风格选择；项目约定或特定界面语境可以覆盖默认规则。

### 结构与句子

- 一个段落承载一个主要信息点。
- 一个句子保持一个清晰的主干；不要连续堆叠多个条件、动作和例外。
- 列表项保持相同层级、句式和信息密度。
- 标题反映用途，不只使用抽象名词。
- 删除重复信息，不删除事实、条件和例外。
- 指代可能不清时，用具体名称替代「该」「其」「此」「上述」。

### 术语与排版

- 同一概念使用同一首选术语，不因追求变化而替换同义词。
- 中文引号默认使用直角引号「」；项目或地区规范可以覆盖。
- 在可见正文中按语义处理中文与英文、独立数字和版本号之间的留白。
- 术语、产品名和缩写优先采用项目规范或官方写法，不机械替换。
- 不直接批量运行排版替换工具；先保护 Markdown、代码和机器可读内容。

详细规则见 [术语与排版](references/terminology-and-typography.md)。

## MarkdownQLite 渲染契约输出补丁

撰写或改写将被 MarkdownQLite 渲染的文档时，读取 [MarkdownQLite 渲染契约](references/render-contract.md) 并执行以下强制项。

**能力目录（能力只存在于此处列出的范围；未列出的写法按不支持处理）：** YAML frontmatter 元数据（`tags` 彩色 chip、`status` chip、`checklist` 清单、`true/false` 复选框、URL 链接值、嵌套分组）、`[TOC]` 目录、标题 `{#id}` 自定义锚点、任务列表三态（`[x]`/`[ ]`/`[-]` 阻塞）、`~~删除线~~`、`==高亮==`、中文着重号自动补配、emoji 短码（精选目录内 332 条）、GFM 提示块（`[!NOTE]`、`[!TIP]`、`[!IMPORTANT]`、`[!WARNING]`、`[!CAUTION]`，可自定义标题）、定义列表（`术语` + `: 定义`）、脚注强能力（多段落、多次引用、独立回跳、悬停预览）、wikilink（同文档标题与同目录文件双解析）、显式 scheme 自动链接、相对链接按文档目录解析、数学公式（`$…$`/`$$…$$` → 原生 MathML，命令范围见契约）、`<details>` 折叠、纯空锚点 `<a id>`。每项能力的可照抄语法模板见渲染契约对应小节——用能力时抄模板，不自造变体。

### 元数据

- 多属性内容（版本、状态、日期、适用范围、来源、关联文档等，两个以上独立属性即触发）必用 YAML frontmatter 元数据标记。
- 字段用英文 snake_case 键，值可用中文；嵌套分组，一组相关属性收拢在同一顶层键下。
- 元数据只写可判真伪的值：日期 `YYYY-MM-DD`、实际版本号、实际相对路径；不写「近期」「最新」等模糊值。

字段词汇与形态细则见 [元数据规则](references/metadata-rules.md)。

### 链接

- 外部链接使用显式 scheme（`https://`、`mailto:` 等）；依赖自动链接的裸域名写法不保证渲染为链接。
- 文档间引用可用 wikilink（`[[目标名]]`）或相对链接；相对路径按文档所在目录解析。
- URL 中的全角标点与尾随括号注意配对，必要时用尖括号包裹或改写目标。

### 数学公式

- 行内公式用 `$…$`，块级公式用 `$$…$$`；只使用渲染契约列出的受支持命令。
- 环境表格、大运算符上下限、数学字体等扩展命令的支持范围以渲染契约为准。

### frontmatter 与结构

- 文档首个块级元素之前不要写会被解析为 frontmatter 内容的行；frontmatter 中的复选框符号与 `data-state` 语义由渲染器处理。
- `[TOC]` 生成目录，标题层级决定目录与侧边栏大纲结构。

完整契约、代码出处与不支持行为清单见 [MarkdownQLite 渲染契约](references/render-contract.md)。

## 按内容类型处理

### 入口页和介绍页

首段优先回答：

- 内容覆盖什么
- 适合谁使用
- 从哪里开始读

避免标题、正文和行动按钮重复同一信息。

### API 文档

- 请求方法、路径、字段和值使用代码环境保护。
- 参数说明一列一义，并写清类型、单位、默认值、限制和是否必填。
- 状态和错误文案根据实际语义翻译，不机械对应单个英文词。
- 写明前置条件、成功结果、失败结果和恢复方式。

详细规则见 [API 状态与错误文案](references/api-status-copy.md)。

### 界面文案

- 按钮说明动作和目标，不重复页面标题。
- 错误提示说明发生了什么、影响是什么，以及如何恢复。
- 危险操作写清对象、后果和是否可撤销。
- 空状态区分「没有数据」「尚未创建」「无权限」和「加载失败」。

### 操作与故障排查

对操作手册、故障排查、运维 Runbook、安全说明和多步骤 API 流程，读取并应用 [受控中文技术写作](references/controlled-technical-chinese.md)。不要把这些规则机械应用于品牌文案、叙事文本或固定引用。

## 项目覆盖规则

先检查目标项目自己的 `AGENTS.md`、术语表、品牌规范、既有文档和用户指示。不要把本 Skill 仓库中的示例约定当成目标项目约定。

需要建立项目覆盖文件时，参考 [项目覆盖模板](references/project-overrides-example.md)，并将实际文件放在目标项目中。

## 编辑流程

1. 确认任务模式、内容类型、受众和项目约定。
2. 标记事实、数字、限制、引用和机器可读内容，建立不可改动边界。
3. 修复语义错误、歧义、遗漏和不一致。
4. 调整信息顺序、段落、标题和列表。
5. 应用 MarkdownQLite 渲染契约输出补丁（元数据、链接、数学、结构）。
6. 处理语气、术语、标点、留白和大小写。
7. 对照原文复核事实、条件、否定范围、因果关系和确定程度。
8. 运行适用的轻量检查器，并人工判断警告和风格提示。

## 最终检查

- 是否新增了来源不明的日期、数字、时限、能力或结论
- 是否遗漏了条件、例外、风险、单位、默认值或恢复方式
- 多属性内容是否使用 frontmatter 元数据标记，值是否可判真伪
- 外部链接是否使用显式 scheme；wikilink 与相对链接目标是否真实存在
- 数学公式是否使用受支持命令集
- 主语、对象、否定范围、因果关系和确定程度是否保持准确
- 同一概念是否使用同一术语
- 标题、正文、卡片和按钮是否重复
- 代码、路径、字段、命令和引用是否保持原样
- 内容是否便于目标读者扫读和执行
- 项目覆盖规则是否来自目标项目，而不是示例文件

## 参考资料路由

- 术语、黑话、错词、大小写、标点和中西文留白：读取 [术语与排版](references/terminology-and-typography.md)
- API 状态、错误码和常见英文状态词：读取 [API 状态与错误文案](references/api-status-copy.md)
- 操作步骤、故障排查、安全说明和运维文档：读取 [受控中文技术写作](references/controlled-technical-chinese.md)
- MarkdownQLite 渲染行为、净化规则、扩展与不支持清单：读取 [MarkdownQLite 渲染契约](references/render-contract.md)
- frontmatter 字段词汇、元数据形态与可校验值：读取 [元数据规则](references/metadata-rules.md)
- 建立目标项目自己的约定：读取 [项目覆盖模板](references/project-overrides-example.md)
